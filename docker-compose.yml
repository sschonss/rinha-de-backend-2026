services:
  haproxy:
    image: haproxy:2.9-alpine
    ports:
      - "9999:9999"
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    depends_on:
      api:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: "0.05"
          memory: "25MB"
    networks:
      - rinha

  api:
    image: sschonss/rinha-api-2026:v6
    platform: linux/amd64
    environment:
      - INDEX_DIR=/data/index
      - LIB_PATH=/app/src/libvector.so
      - NPROBE=5
      - WORKERS=2
      - PORT=9999
    deploy:
      resources:
        limits:
          cpus: "0.95"
          memory: "325MB"
    networks:
      - rinha

networks:
  rinha:
    driver: bridge
